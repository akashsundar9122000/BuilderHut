# dependencies
node_modules
.pnpm-store

# next
.next
out
next-env.d.ts
*.tsbuildinfo

# env — never commit secrets
.env
.env.*
!.env.example

# deploy
.vercel

# test artefacts
test-results
playwright-report
blob-report
coverage
.playwright

# local media fallback (before R2 is configured)
.uploads

# os
.DS_Store

# Capture-run scratch: the journey context, the merchant session, generated artwork.
.guide-shots
